{
  "name": "@electrostock/mobile",
  "version": "1.0.0",
  "description": "ElectroStock Pro Mobile App",
  "main": "expo-router/entry",
  "scripts": {
    "start": "expo start",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "web": "expo start --web",
    "build": "expo build",
    "test": "bun test",
    "type-check": "tsc --noEmit",
    "lint": "eslint src/**/*.{ts,tsx}"
  },
  "dependencies": {
    "@electrostock/types": "workspace:*",
    "@electrostock/utils": "workspace:*",
    "@electrostock/constants": "workspace:*",
    "expo": "~51.0.0",
    "react": "18.3.1",
    "react-native": "0.74.5"
  },
  "devDependencies": {
    "@types/react": "~18.3.0",
    "typescript": "^5.3.0"
  }
}
