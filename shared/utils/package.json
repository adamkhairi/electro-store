{
  "name": "@electrostock/utils",
  "version": "1.0.0",
  "description": "Shared utility functions for ElectroStock Pro",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "bun run clean && tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "test": "bun test",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@electrostock/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
